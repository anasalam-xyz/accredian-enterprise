import Hero from "@/components/Hero";
import Stats from "@/components/Hero";
import Clients from "@/components/Hero";

export default function Home() {
  return (
    <main className="my-20">
      <section id="home">
        <Home />
      </section>

      <section id="stats">
        <Stats />
      </section>

      <section id="clients">
        <Clients />
      </section>
    </main>
  );
}
